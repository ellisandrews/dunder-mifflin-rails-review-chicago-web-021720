<h1>Edit Employee</h1>

<% if @errors %>
    <% @errors.each do |error| %>
        <p>Error: <%= error %></p>
    <% end %>
<% end %>

<!-- Based on what we talked about today, you could refactor to render this as a partial with local variables -->
<%= render 'form' %>

<br>

<%= link_to "Back to #{@employee.name}", employee_path(@employee) %><br>
<%= link_to 'Back to Employees', employees_path %>
